# But Dad Spec

## Loop summary
- Writer turns: 6/6
- Coach turns: 6/6

## Current spec
## Objective
Issue #10 reusable MCP tool sample run

## Preferred model backend
Malachi

## Scope
- Produce a single living spec artifact after writer turn 6.
- Preserve a readable transcript for every writer and coach turn.
- Keep the loop bounded and deterministic for local verification.

## Working requirements
- Expose a reusable MCP tool surface that callers can invoke locally.
- Write a final spec artifact plus transcript artifacts to a predictable directory.
- Reflect the latest coach recommendation in this draft: Call out deterministic overwrite behavior, test the artifact contract, and keep sources empty unless live research actually ran.

## Acceptance criteria
- A caller can run the loop locally and inspect the final spec.
- Artifacts are written under a stable output directory.
- Tests cover the core execution path and output contract.

## Edge cases
- Re-running the same spec loop name should overwrite artifacts deterministically.
- Empty live research should not invent sources in preview mode.
- Writer and coach turns must stop at the configured limits.

## Changelog
- Turn 6: tightened the draft around the latest coach recommendation.

## Draft history

### Draft 1

## Objective
Issue #10 reusable MCP tool sample run

## Preferred model backend
Malachi

## Scope
- Produce a single living spec artifact after writer turn 1.
- Preserve a readable transcript for every writer and coach turn.
- Keep the loop bounded and deterministic for local verification.

## Working requirements
- Expose a reusable MCP tool surface that callers can invoke locally.
- Write a final spec artifact plus transcript artifacts to a predictable directory.
- Reflect the latest coach recommendation in this draft: Define the first living draft.

## Acceptance criteria
- A caller can run the loop locally and inspect the final spec.
- Artifacts are written under a stable output directory.
- Tests cover the core execution path and output contract.

## Edge cases
- Re-running the same spec loop name should overwrite artifacts deterministically.
- Empty live research should not invent sources in preview mode.
- Writer and coach turns must stop at the configured limits.

## Changelog
- Turn 1: tightened the draft around the latest coach recommendation.

#### Rationale
- Preserve a single living specification instead of resetting between turns.
- Use writer turn 1 to absorb the latest coach feedback: Define the first living draft.

### Draft 2

## Objective
Issue #10 reusable MCP tool sample run

## Preferred model backend
Malachi

## Scope
- Produce a single living spec artifact after writer turn 2.
- Preserve a readable transcript for every writer and coach turn.
- Keep the loop bounded and deterministic for local verification.

## Working requirements
- Expose a reusable MCP tool surface that callers can invoke locally.
- Write a final spec artifact plus transcript artifacts to a predictable directory.
- Reflect the latest coach recommendation in this draft: Add exact output filenames, keep the loop bounded, and document how callers verify the result locally.

## Acceptance criteria
- A caller can run the loop locally and inspect the final spec.
- Artifacts are written under a stable output directory.
- Tests cover the core execution path and output contract.

## Edge cases
- Re-running the same spec loop name should overwrite artifacts deterministically.
- Empty live research should not invent sources in preview mode.
- Writer and coach turns must stop at the configured limits.

## Changelog
- Turn 2: tightened the draft around the latest coach recommendation.

#### Rationale
- Preserve a single living specification instead of resetting between turns.
- Use writer turn 2 to absorb the latest coach feedback: Add exact output filenames, keep the loop bounded, and document how callers verify the result locally.

### Draft 3

## Objective
Issue #10 reusable MCP tool sample run

## Preferred model backend
Malachi

## Scope
- Produce a single living spec artifact after writer turn 3.
- Preserve a readable transcript for every writer and coach turn.
- Keep the loop bounded and deterministic for local verification.

## Working requirements
- Expose a reusable MCP tool surface that callers can invoke locally.
- Write a final spec artifact plus transcript artifacts to a predictable directory.
- Reflect the latest coach recommendation in this draft: Call out deterministic overwrite behavior, test the artifact contract, and keep sources empty unless live research actually ran.

## Acceptance criteria
- A caller can run the loop locally and inspect the final spec.
- Artifacts are written under a stable output directory.
- Tests cover the core execution path and output contract.

## Edge cases
- Re-running the same spec loop name should overwrite artifacts deterministically.
- Empty live research should not invent sources in preview mode.
- Writer and coach turns must stop at the configured limits.

## Changelog
- Turn 3: tightened the draft around the latest coach recommendation.

#### Rationale
- Preserve a single living specification instead of resetting between turns.
- Use writer turn 3 to absorb the latest coach feedback: Call out deterministic overwrite behavior, test the artifact contract, and keep sources empty unless live research actually ran.

### Draft 4

## Objective
Issue #10 reusable MCP tool sample run

## Preferred model backend
Malachi

## Scope
- Produce a single living spec artifact after writer turn 4.
- Preserve a readable transcript for every writer and coach turn.
- Keep the loop bounded and deterministic for local verification.

## Working requirements
- Expose a reusable MCP tool surface that callers can invoke locally.
- Write a final spec artifact plus transcript artifacts to a predictable directory.
- Reflect the latest coach recommendation in this draft: Call out deterministic overwrite behavior, test the artifact contract, and keep sources empty unless live research actually ran.

## Acceptance criteria
- A caller can run the loop locally and inspect the final spec.
- Artifacts are written under a stable output directory.
- Tests cover the core execution path and output contract.

## Edge cases
- Re-running the same spec loop name should overwrite artifacts deterministically.
- Empty live research should not invent sources in preview mode.
- Writer and coach turns must stop at the configured limits.

## Changelog
- Turn 4: tightened the draft around the latest coach recommendation.

#### Rationale
- Preserve a single living specification instead of resetting between turns.
- Use writer turn 4 to absorb the latest coach feedback: Call out deterministic overwrite behavior, test the artifact contract, and keep sources empty unless live research actually ran.

### Draft 5

## Objective
Issue #10 reusable MCP tool sample run

## Preferred model backend
Malachi

## Scope
- Produce a single living spec artifact after writer turn 5.
- Preserve a readable transcript for every writer and coach turn.
- Keep the loop bounded and deterministic for local verification.

## Working requirements
- Expose a reusable MCP tool surface that callers can invoke locally.
- Write a final spec artifact plus transcript artifacts to a predictable directory.
- Reflect the latest coach recommendation in this draft: Call out deterministic overwrite behavior, test the artifact contract, and keep sources empty unless live research actually ran.

## Acceptance criteria
- A caller can run the loop locally and inspect the final spec.
- Artifacts are written under a stable output directory.
- Tests cover the core execution path and output contract.

## Edge cases
- Re-running the same spec loop name should overwrite artifacts deterministically.
- Empty live research should not invent sources in preview mode.
- Writer and coach turns must stop at the configured limits.

## Changelog
- Turn 5: tightened the draft around the latest coach recommendation.

#### Rationale
- Preserve a single living specification instead of resetting between turns.
- Use writer turn 5 to absorb the latest coach feedback: Call out deterministic overwrite behavior, test the artifact contract, and keep sources empty unless live research actually ran.

### Draft 6

## Objective
Issue #10 reusable MCP tool sample run

## Preferred model backend
Malachi

## Scope
- Produce a single living spec artifact after writer turn 6.
- Preserve a readable transcript for every writer and coach turn.
- Keep the loop bounded and deterministic for local verification.

## Working requirements
- Expose a reusable MCP tool surface that callers can invoke locally.
- Write a final spec artifact plus transcript artifacts to a predictable directory.
- Reflect the latest coach recommendation in this draft: Call out deterministic overwrite behavior, test the artifact contract, and keep sources empty unless live research actually ran.

## Acceptance criteria
- A caller can run the loop locally and inspect the final spec.
- Artifacts are written under a stable output directory.
- Tests cover the core execution path and output contract.

## Edge cases
- Re-running the same spec loop name should overwrite artifacts deterministically.
- Empty live research should not invent sources in preview mode.
- Writer and coach turns must stop at the configured limits.

## Changelog
- Turn 6: tightened the draft around the latest coach recommendation.

#### Rationale
- Preserve a single living specification instead of resetting between turns.
- Use writer turn 6 to absorb the latest coach feedback: Call out deterministic overwrite behavior, test the artifact contract, and keep sources empty unless live research actually ran.

## Coach critiques

### Critique 1

- Claim: The first draft still needs sharper acceptance criteria and explicit artifact paths.
- Recommendation: Add exact output filenames, keep the loop bounded, and document how callers verify the result locally.

### Critique 2

- Claim: The draft is improving, but it still needs tighter failure-path handling and clearer verification steps.
- Recommendation: Call out deterministic overwrite behavior, test the artifact contract, and keep sources empty unless live research actually ran.

### Critique 3

- Claim: The draft is improving, but it still needs tighter failure-path handling and clearer verification steps.
- Recommendation: Call out deterministic overwrite behavior, test the artifact contract, and keep sources empty unless live research actually ran.

### Critique 4

- Claim: The draft is improving, but it still needs tighter failure-path handling and clearer verification steps.
- Recommendation: Call out deterministic overwrite behavior, test the artifact contract, and keep sources empty unless live research actually ran.

### Critique 5

- Claim: The draft is improving, but it still needs tighter failure-path handling and clearer verification steps.
- Recommendation: Call out deterministic overwrite behavior, test the artifact contract, and keep sources empty unless live research actually ran.

### Critique 6

- Claim: The loop has reached the configured bound; ship the current draft with the recorded transcript and noted deviations.
- Recommendation: Stop iterating, publish the final artifact set, and carry the preferred Malachi backend forward as metadata for future live wiring.

## Source appendix

_No sources captured._
